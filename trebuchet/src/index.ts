import { partOne } from "./partOne";
import { partTwo } from "./partTwo";

// For both parts of the task I assumed that there is always at least one valid digit
console.log(`Result for part one: ${partOne()}`);
console.log(`Result for part two: ${partTwo()}`);
